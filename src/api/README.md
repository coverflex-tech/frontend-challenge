# API scaffold

This folder provides API contracts and endpoint constants for the challenge.

Suggested endpoints:

- `GET /benefits`
- `GET /employees/:id/benefits`

Candidates can choose any data integration strategy during the interview:

- native `fetch`
- an HTTP client (for example, `axios`)
- a local adapter that returns data from the TypeScript mock files
